/**
 * Kept for compatibility — Settings now uses MailboxConnectPanel.
 * Re-exports the multi-provider panel so older imports keep working.
 */
export { MailboxConnectPanel as GmailConnectPanel } from "@/components/mail/mailbox-connect-panel";
